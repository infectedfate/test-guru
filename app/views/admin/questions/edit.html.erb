<div class="row justify-content-center mt-3">
    <div class="col col-md-4">
        ß<%= question_header(@question) %>
        <div class="card border-primary">
            <div class="card-body">
                <%= render 'form' %>
            </div>
                <%= link_to t('.back'), admin_test_path %>
        </div>
    </div>
</div>