<tr>
  <td> <%= test.title %> </td>
  <td> <%= test_level(test) %> </td>
  <td> <%= link_to 'Вход', test_path(test) %> </td>
  <td> <%= link_to 'Редактировать', edit_test_path(test) %> </td>
  <td> <%= link_to 'Удалить',
                    test_path(test),
                    method: :delete,
                    data: { confirm: 'Are you shure?' } %>
  </td>
  <td align="center">Количество вопросов: <%= test.questions.count %></td>
</tr>
